#!/bin/sh

# Dependencies:
# convert wal xdotool xwallpaper 

wall_dir=~/pix/wall

if [ ! -z "$1" ]; then
  wall=$(
      find "$wall_dir" -type f \( -name "*.png" -o -name "*.jpg" \) |\
      shuf -n1
    )
else
  wall="$1"
if

convert "$wall" ~/.config/wall.jpg
xwallpaper --zoom ~/.config/wall.jpg
wal -c
wal -i ~/.config/wall.jpg
xdotool key super+F5
